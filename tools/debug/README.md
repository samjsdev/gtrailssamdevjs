Debug and one-off tooling.

- `google-maps/` contains scraping and browser debugging scripts.
- `artifacts/` contains screenshots and scratch files produced while debugging.
- `template-maintenance/` contains old template maintenance helpers.

Production template routes should stay under `app/clinicwebsite/`.
